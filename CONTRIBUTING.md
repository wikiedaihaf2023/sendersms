# 🤝 المساهمة في MessageFlow Pro

شكراً لاهتمامك بالمساهمة! إليك طريقة البدء:

## خطوات المساهمة

1. **افتح Issue** لوصف المشكلة أو الميزة المقترحة.
2. **اعمل Fork** للمستودع ثم `git clone`.
3. **أنشئ فرعاً** جديداً: `git checkout -b feature/اسم-الميزة`.
4. **نفّذ التغييرات** مع الالتزام بأسلوب الكود الحالي.
5. **شغّل الاختبارات**: `pytest tests/`.
6. **ارفع Pull Request** مع وصف واضح للتغيير.

## إرشادات الكود

- استخدم اللغة العربية في رسائل السجلات والواجهات.
- أضف docstrings لجميع الدوال والفئات.
- حافظ على تنسيق الكود باستخدام `black`.

## إضافة مزود إرسال جديد

1. أنشئ خدمة جديدة في `app/services/`.
2. ورّث نفس واجهة `SMSService` / `WhatsAppService` (دالة `send(message)`).
3. أضف الإعدادات في `app/core/config.py` و `.env.example`.
4. أضف الاختبارات في `tests/`.
